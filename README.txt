For compiling we use : g++ -Werror -Wall -O -o test *.cpp
For running a test : ./test unitTests/<filename.input> unitTests/<fileout.gameout>
 
We were having issues with the makefile and <> in the terminal input so due to time constraints could not fix them.